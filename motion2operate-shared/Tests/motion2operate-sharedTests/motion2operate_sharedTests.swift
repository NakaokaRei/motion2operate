import XCTest
@testable import motion2operate_shared

final class motion2operate_sharedTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(motion2operate_shared().text, "Hello, World!")
    }
}
